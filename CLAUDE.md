# Claude Code entry point

Read and follow [`AGENTS.md`](AGENTS.md), the single repository contract for architecture, React/SSR
behavior, testing, documentation and release safety. Use the installed Silen Agent Contract for the
documentation API and run `pnpm docs:mcp` only in its default read-only mode.
